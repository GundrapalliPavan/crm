name: Frontend CI

on:
  pull_request:
    paths:
      - "frontend/**"
      - "pnpm-workspace.yaml"
      - "tsconfig.base.json"
  push:
    branches: [main]
    paths:
      - "frontend/**"

jobs:
  lint-typecheck-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile

      - run: pnpm --filter @crm/frontend lint
      - run: pnpm --filter @crm/frontend typecheck
      - run: pnpm --filter @crm/frontend build
